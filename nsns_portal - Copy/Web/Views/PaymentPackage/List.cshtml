@model IEnumerable<PaymentPackage>

@{
    Layout = "~/Views/Shared/_Layout.cshtml";

    ViewData["Title"] = "Manage Payment Packages";
}

<h2>Manage Payment Packages</h2>

<!-- ✅ Add New Package Button -->
<a href="/PaymentPackage/AddEdit" style="display: inline-block; margin-bottom: 20px;">Add Payment Package</a>

<!-- ✅ Table Showing Payment Packages -->
<table style="border-collapse: collapse; width: 100%; text-align: left;">
    <thead>
        <tr style="background-color: #f2f2f2;">
            <th style="padding: 8px; border: 1px solid #ddd;">Title</th>
            <th style="padding: 8px; border: 1px solid #ddd;">Description</th>
            <th style="padding: 8px; border: 1px solid #ddd;">Amount</th>
            <th style="padding: 8px; border: 1px solid #ddd;">Active</th>
            <th style="padding: 8px; border: 1px solid #ddd;">Actions</th>
        </tr>
    </thead>
    <tbody>
        @foreach (var package in Model)

        {
            <tr>
                <td style="padding: 8px; border: 1px solid #ddd;">@package.Title</td>
                <td style="padding: 8px; border: 1px solid #ddd;">@package.Description</td>
                <td style="padding: 8px; border: 1px solid #ddd;">$@package.Amount</td>
                <td style="padding: 8px; border: 1px solid #ddd;">@(package.IsActive ? "Yes" : "No")</td>
                <td style="padding: 8px; border: 1px solid #ddd;">
                    <a href="/PaymentPackage/AddEdit/@package.PackageID" style="margin-right: 10px; color: #007bff; text-decoration: none;">Edit</a>
                    <a href="/PaymentPackage/ConfirmDelete/@package.PackageID" style="color: #007bff; text-decoration: none;">Delete</a>
                </td>
            </tr>
        }

        @if (!Model.Any())

        {
            <tr>
                <td colspan="5" style="text-align: center; padding: 10px;">No payment packages found.</td>
            </tr>
        }

        @if (TempData["SuccessMessage"] != null)

        {
            <tr>
                <td colspan="5">
                    <div class="alert alert-success">
                        @TempData["SuccessMessage"]
                    </div>
                </td>
            </tr>
        }

        @if (TempData["ErrorMessage"] != null)

        {
            <tr>
                <td colspan="5">
                    <div class="alert alert-danger">
                        @TempData["ErrorMessage"]
                    </div>
                </td>
            </tr>
        }
    </tbody>
</table>